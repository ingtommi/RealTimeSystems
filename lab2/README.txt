TO DO THINGS:

- CLOCK TASK: 
-- handle alarms (use ALAH, ALAM, ALAS)

- SENSOR TASK:
-- periodic monitoring (can be with delay, but if 0 must be only on demand)
-- on demand monitoring
-- use potentiometer
-- save values in records
-- handle alarms (use ALAT, ALAL)

- USER INTERFACE:
-- LCD

- PROCESSING TASK:
-- perioding and on demand processing 
-- represent values on RGB and normal LEDs
-- determine max, min, mean values of given records and send them to console
